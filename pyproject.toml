[tool.poetry]
name = "cs160"
version = "0.1.0"
description = ""
authors = ["pocafup <pocafup@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
